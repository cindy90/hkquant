# ============================================================
# iFinD (同花顺 QuantAPI) 凭证模板 —— 复制为 .env 后填入实际值
# 文档: https://quantapi.51ifind.com/gwstatic/static/ds_web/quantapi-web/help-center/deploy.html
# ============================================================

# SDK 接口 (iFinDPy.THS_iFinDLogin) 用账号密码
IFIND_USERNAME=your_username_here
IFIND_PASSWORD=your_password_here

# HTTP REST 接口 (用于 report_query 公告查询等 SDK 未覆盖端点)
# 获取方式 (二选一):
#   A. iFinD 超级命令客户端 → 工具 → refresh_token 查询
#   B. 网页版超级命令-账号详情:
#      https://quantapi.10jqka.com.cn/gwstatic/static/ds_web/super-command-web/index.html#/AccountDetails
# 有效期与账号到期日同步, 更新后旧 access_token 全部失效.
IFIND_REFRESH_TOKEN=

# 可选: 接口地址覆盖 (默认 https://quantapi.51ifind.com)
# IFIND_API_HOST=

# 可选: 覆盖输出目录（默认写到 项目根/data/raw/ifind/）
# IFIND_OUTPUT_DIR=
